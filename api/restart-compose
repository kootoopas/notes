#/bin/bash

docker build . -t notes-api --rm
docker-compose down
docker-compose up -d
sleep 0.5
curl -d '{"title":"","body":""}' -H 'Content-Type: application/json' http://localhost:8000/api/v1/notes
